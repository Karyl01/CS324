# YOLOv10n 手势识别项目

基于YOLOv10n的实时手势识别系统，支持8种基本手势的检测和分类。

## 项目特点

- 🚀 **最新模型**: 使用YOLOv10n - 最新一代目标检测模型
- 🎯 **手势识别**: 支持8种基本手势类别
- 📹 **实时检测**: 支持摄像头实时手势检测
- 🎨 **可视化**: 丰富的检测结果可视化
- ⚙️ **易于使用**: 简洁的训练和推理接口
- 📊 **数据分析**: 完整的数据集分析和验证工具

## 支持的手势类别

| 类别ID | 手势名称 | 描述 |
|--------|----------|------|
| 0 | up | 向上 |
| 1 | down | 向下 |
| 2 | left | 向左 |
| 3 | right | 向右 |
| 4 | front | 向前 |
| 5 | back | 向后 |
| 6 | clockwise | 顺时针 |
| 7 | anticlockwise | 逆时针 |

## 项目结构

```
yolo10/
├── README.md                    # 项目说明文档
├── requirements.txt             # 项目依赖
├── train_yolov10n.py           # 训练脚本
├── predict_yolov10n.py         # 摄像头推理脚本
├── models/
│   └── yolov10n_custom.py      # 自定义模型实现
├── utils/
│   ├── config.py               # 配置文件
│   ├── convert_voc_to_yolo.py  # 数据格式转换
│   ├── transforms.py           # 数据增强工具
│   └── dataset.py              # 数据集处理工具
└── data/
    ├── dataset.yaml            # 数据集配置
    ├── images/                 # 图像文件
    └── labels/                 # YOLO格式标签文件
```

## 环境要求

- Python >= 3.8
- PyTorch >= 1.9.0
- CUDA >= 11.1 (GPU训练推荐)
- 内存 >= 8GB
- 显存 >= 4GB (GPU训练)

## 安装步骤

### 1. 克隆项目
```bash
git clone <repository-url>
cd yolo10
```

### 2. 创建虚拟环境
```bash
conda create -n yolov10n-gesture python=3.8
conda activate yolov10n-gesture
```

### 3. 安装依赖
```bash
pip install -r requirements.txt
```

### 4. 数据准备
如果已有数据集，跳过此步骤。否则需要：
```bash
# 运行数据格式转换（从VOC到YOLO格式）
python utils/convert_voc_to_yolo.py

# 验证数据集完整性
python utils/dataset.py
```

## 快速开始

### 1. 训练模型

```bash
# 基础训练
python train_yolov10n.py --model n --epochs 100 --batch 16

# 自定义参数训练
python train_yolov10n.py \
    --model n \
    --data data/dataset.yaml \
    --epochs 150 \
    --batch 32 \
    --imgsz 416 \
    --lr 0.01 \
    --device 0 \
    --name my_experiment
```

### 2. 摄像头实时检测

```bash
# 使用训练好的模型进行实时检测
python predict_yolov10n.py \
    --model runs/detect/train/weights/best.pt \
    --camera 0 \
    --conf 0.5 \
    --iou 0.45
```

### 3. 模型验证

```bash
# 验证模型性能
python -m ultralytics.models.yolo val \
    model=runs/detect/train/weights/best.pt \
    data=data/dataset.yaml
```

## 训练参数说明

### 基础参数
- `--model`: 模型大小 (n/s/m/b/l/x)
- `--data`: 数据集配置文件路径
- `--epochs`: 训练轮数
- `--batch`: 批量大小
- `--imgsz`: 输入图像尺寸
- `--lr`: 学习率

### 高级参数
- `--optimizer`: 优化器类型 (SGD/Adam/AdamW)
- `--patience`: 早停耐心值
- `--device`: 训练设备 (cpu/cuda/auto)
- `--augment`: 启用数据增强
- `--pretrained`: 使用预训练权重

## 推理参数说明

### 基础参数
- `--model`: 训练好的模型路径
- `--camera`: 摄像头设备ID
- `--conf`: 置信度阈值
- `--iou`: NMS IOU阈值

### 显示参数
- `--resolution`: 摄像头分辨率 (格式: WxH)
- `--fullscreen`: 全屏显示
- `--save-dir`: 截图保存目录

## 数据集格式

项目使用YOLO格式的数据集：

### 图像文件
- 支持格式: JPG, PNG, JPEG
- 存放位置: `data/images/`

### 标签文件
- 格式: `.txt` 文件
- 每行格式: `class_id x_center y_center width height`
- 坐标均为归一化值 (0-1)
- 存放位置: `data/labels/`

### 标签示例
```
0 0.5 0.5 0.8 0.6   # 类别0，中心点(0.5,0.5)，宽0.8，高0.6
3 0.3 0.7 0.2 0.4   # 类别3，中心点(0.3,0.7)，宽0.2，高0.4
```

## 模型性能

基于YOLOv10n的手势识别模型性能：

| 模型 | mAP@0.5 | 参数量 | 推理速度 (FPS) | 模型大小 |
|------|---------|--------|----------------|----------|
| YOLOv10n | TBD | 2.2M | 100+ | 6.1MB |
| YOLOv10s | TBD | 7.2M | 80+ | 21.5MB |

*注: 性能数据基于内部测试，实际性能可能因硬件和数据集而异*

## 常见问题

### Q: 训练时显存不足怎么办？
A: 可以尝试以下方法：
- 减小批量大小 (`--batch`)
- 使用更小的图像尺寸 (`--imgsz`)
- 使用混合精度训练 (在config.py中设置)

### Q: 如何提高检测精度？
A: 建议尝试：
- 增加训练数据量
- 使用数据增强 (`--augment`)
- 调整置信度和IOU阈值
- 使用更大的模型 (YOLOv10s/m)

### Q: 摄像头检测延迟高怎么办？
A: 可以尝试：
- 降低输入分辨率
- 使用更小的模型
- 关闭不必要的可视化效果

## 开发指南

### 添加新的手势类别

1. 更新 `utils/config.py` 中的 `NUM_CLASSES` 和 `CLASS_NAMES`
2. 重新准备数据集并转换格式
3. 重新训练模型

### 自定义数据增强

在 `utils/transforms.py` 中添加新的增强方法：

```python
def custom_transform(self, image):
    # 自定义增强逻辑
    return enhanced_image
```

### 模型导出

```bash
# 导出ONNX格式
python -c "
from models.yolov10n_custom import create_model
model = create_model('n', pretrained=False)
model.export(format='onnx')
"
```

## 贡献指南

欢迎提交Issue和Pull Request！

### 开发环境设置
```bash
# 安装开发依赖
pip install -r requirements.txt

# 运行测试
pytest tests/

# 代码格式化
black .
flake8 .
```

## 许可证

本项目采用MIT许可证 - 详见 [LICENSE](LICENSE) 文件

## 致谢

- [Ultralytics YOLOv10](https://github.com/THU-MIG/yolov10) - 核心模型框架
- [Original YoloGesture Project](https://github.com/Kedreamix/YoloGesture) - 原始数据集和灵感来源

## 联系方式

如有问题或建议，请通过以下方式联系：
- 提交 GitHub Issue
- 发送邮件至 [your-email@example.com]

---

**注意**: 本项目仅用于学习和研究目的，请勿用于商业用途。